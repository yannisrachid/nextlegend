# Handoff - 2026-03-11

## Objectif de la session
Mettre en production le flux sans relancer un scraping historique complet:
- charger les saisons passées depuis les CSV existants,
- garder un cron hebdo uniquement pour la saison courante (`2025/2026`),
- monitorer via la Home page (pas de dépendance SMTP).

## Décisions validées
- Pas de relance de scraping pour les saisons antérieures.
- Backfill DB VPS depuis les fichiers `data/wyscout_players_*_final.csv`.
- Cron hebdo pour le job complet current season uniquement.
- Monitoring principal via Home (`Pipeline status`), emails désactivés (`SKIP_EMAIL_ALERTS=1`).

## Statut actuel (à appliquer pour les prochains process)
- Backfill historique: terminé (one-shot).
- Désormais: aucune copie CSV manuelle en routine.
- Routine unique: cron du lundi matin sur VPS pour la saison actuelle seulement.

## État confirmé
- Chemin repo VPS utilisé: `~/nextlegend/new_nextlegend`.
- Cron hebdo en place: lundi 07:00 (heure serveur), avec lock `flock`.

## Historique one-shot (déjà fait, ne pas rejouer en routine)
Les commandes ci-dessous sont archivées pour traçabilité uniquement:
- migration initiale des CSV historiques,
- correction de permissions,
- backfill initial.

Copie CSV depuis local:
```bash
rsync -avz --progress -e "ssh -i ~/ssh_vps" /Users/yannis/ylfc/new_nextlegend/data/wyscout_players_{2022_2023,2023_2024,2024_2025,2025,2025_2026}_final.csv yannis@46.224.49.46:~/nextlegend/new_nextlegend/data/
```

## Incident rencontré et correctif
Erreur:
- `mkstemp ... Permission denied` pendant `rsync`.

Cause:
- dossier `data` sur VPS possédé par `root`.

Correctif VPS:
```bash
cd ~/nextlegend/new_nextlegend
ls -ld . data
sudo chown -R yannis:yannis data
```

## Backfill DB sur VPS (sans scraping)
```bash
cd ~/nextlegend/new_nextlegend
chmod +x scripts/backfill_vps_from_existing_csvs.sh
DOCKER_COMPOSE_FILE=infra/compose/docker-compose-prod.yml ./scripts/backfill_vps_from_existing_csvs.sh
```

Dry-run:
```bash
cd ~/nextlegend/new_nextlegend
DOCKER_COMPOSE_FILE=infra/compose/docker-compose-prod.yml DRY_RUN=1 ./scripts/backfill_vps_from_existing_csvs.sh
```

## Cron current season (hebdo)
Entrée correcte:
```cron
0 7 * * 1 cd /home/yannis/nextlegend/new_nextlegend/wyscout_current_season_job && /usr/bin/flock -n /tmp/nextlegend_current_season.lock DOCKER_COMPOSE_FILE=/home/yannis/nextlegend/new_nextlegend/infra/compose/docker-compose-prod.yml SKIP_EMAIL_ALERTS=1 PIPELINE_REPLACE_INPUT_SLICES=1 PIPELINE_REPLACE_SIMILARITY=1 ./run_current_season_e2e.sh >> logs/cron_current_season.log 2>&1
```

Point d’attention:
- `flock -n` est correct.
- `flock-n` est invalide.

Vérifier cron:
```bash
crontab -l
```

## Monitoring
- Home page affiche `Pipeline status` + statut de chargement saison courante.
- Logs cron:
  - `wyscout_current_season_job/logs/cron_current_season.log`
  - `wyscout_current_season_job/logs/current_season_e2e_*.log`

## Procédure de reprise rapide
1) Local: `git push`.
2) VPS:
```bash
cd ~/nextlegend/new_nextlegend
git pull
```
3) Vérifier `crontab -l` (une seule ligne current season).
4) Vérifier la Home (`Pipeline status`) et les logs.
